<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jielong.core.dao.GoodsClassDao">
    <select id="selectById" resultType="GoodsClass">
        select * from goods_class where id = #{id}
    </select>
</mapper>
